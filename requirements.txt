# ============================================================
# requirements.txt
# CryptoAPI — Cryptographic Security & Data Integrity Service
#
# ⚠️  Python 3.10 REQUIRED (pinned in pyproject.toml)
#     Other versions are not guaranteed to work.
#
# Recommended: use `uv` for reproducible installs
#   pip install uv && uv sync
#
# Standard pip install:
#   pip install -r requirements.txt
# ============================================================

# ── Web Framework ────────────────────────────────────────────
fastapi==0.104.1
uvicorn==0.24.0                  # ASGI server to run FastAPI

# ── Data Validation ──────────────────────────────────────────
pydantic==2.5.0

# ── Cryptography ─────────────────────────────────────────────
cryptography>=46.0.3             # RSA-PSS, ECDSA (secp256k1), Ed25519

# ── Form / File Upload Support ───────────────────────────────
python-multipart>=0.0.20         # Required for FastAPI Form() and File() parsing

# ── Environment Variables ────────────────────────────────────
python-dotenv==1.0.0

# ============================================================
# Development / Testing (optional)
# ============================================================
# httpx>=0.25.0                  # For testing FastAPI endpoints with TestClient
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
